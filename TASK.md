# 待完成的任务列表

1. header上面的项目下拉框选择没效果，选择项目后 仪表板 看板 员工都要根据选择项目显示 ok
2. 看板点击运行任务、但是任务都是同一个员工的情况下 运行状态需要变成 排队中
3. 仪表盘最近活动显示的是key而不是中文、还需要增加一个查看更多的功能可以分页查询
4. 检查仪表板员工绩效功能是干嘛的？ ok
5. 看板 运行任务的时候检查下停止任务是否有效果 ok
6. 员工默认是在线状态，修改员工的时候可以修改员工在线或者离线，离线的员工不能运行任务
7. 设置界面增加数据库备份、恢复功能
8. 支持Codex exec的同时也要支持Codex SDK,可以再设置中添加一个Codex SDK的配置，安装SDK，显示SDK版本
9. 看板 任务详情 增加一个AI生成计划，要达到codex /plan的效果，生成计划后返回结果，然后显示插入详情按钮，插入详情需要弹框确认是否替换详情
10. AI建议指派 返回结果 只返回了员工的id，没返回员工名称
11. 看板 审核中的任务 添加一个审核代码的功能
